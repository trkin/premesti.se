<%
  breadcrumb t('dashboard') => dashboard_path, Move.model_name.human => nil
%>

<%= @move.from_group.age %>
<%= @move.from_group.location.name %>
<%= @move.prefered_groups.each do |group| %>
  --> <%= group.location.name %>
  <br>
<% end %>
